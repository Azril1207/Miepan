<!DOCTYPE html>
<html>
<head>
<title>Edit Profile</title>
<style>
body {
  font-family: sans-serif;
}

.container {
  width: 400px;
  margin: 50px auto;
  padding: 20px;
  border: 1px solid #ccc;
  border-radius: 5px;
}

.profile-pic {
  width: 100px;
  height: 100px;
  margin: 0 auto;
  border-radius: 50%;
  background-color: #ddd;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 3em;
  color: #666;
}

.input-field {
  margin-bottom: 15px;
}

.input-field label {
  display: block;
  margin-bottom: 5px;
}

.input-field input {
  width: 100%;
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 3px;
}

.buttons {
  display: flex;
  justify-content: space-between;
}

.button {
  padding: 10px 20px;
  border: none;
  border-radius: 3px;
  cursor: pointer;
}

.button-cancel {
  background-color: #ccc;
}

.button-save {
  background-color: #4CAF50;
  color: white;
}
</style>
</head>
<body>

<div class="container">
  <h2>Edit Profile</h2>

  <div class="profile-pic">
    <i class="fas fa-user-circle"></i>
  </div>

  <div class="input-field">
    <label for="name">Nama</label>
    <input type="text" id="name" name="name">
  </div>

  <div class="input-field">
    <label for="email">Email</label>
    <input type="email" id="email" name="email">
  </div>

  <div class="input-field">
    <label for="phone">No telpon</label>
    <input type="text" id="phone" name="phone">
  </div>

  <div class="input-field">
    <label for="address">alamat</label>
    <input type="text" id="address" name="address">
  </div>

  <div class="buttons">
    <button class="button button-cancel" type="button">batal</button>
    <button class="button button-save" type="button">perbarui</button>
  </div>
</div>

</body>
</html>
